// define r = 4 and h = 7, V = 0 (to initialise)

let r = 4.5;
let h = 7;
let V = 0;

// use cone volume formula to calculate volume
// V= (h/3)πr^2

// use Math.PI for pi
// use Math.pow for power


V = ( h/3 * Math.PI * Math.pow(r, 2));

console.log(V);
