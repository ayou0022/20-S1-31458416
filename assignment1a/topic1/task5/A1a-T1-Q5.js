// topic 1 task 5

// define a = 1, b = 2, tmp = 0

let a = 1;
let b = 2;
let tmp = 0;

// swap the two variables using tmp
// ie. move a into tmp, b into a, tmp into b

tmp = a;
a = b;
b = tmp;
